.git
.github
frontend/node_modules
frontend/tsconfig.tsbuildinfo
dist
build
*.egg-info
**/__pycache__
*.py[cod]
dev/data
data
.DS_Store
.venv
venv

# Built by the bundle stage of deploy/Dockerfile. Letting the host's copy into
# the context would only shadow it, which is the whole failure this arrangement
# exists to prevent.
pretix_openpos/static/pretix_openpos/pwa
